apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastapi-app
  template:
    metadata:
      labels:
        app: fastapi-app
    spec:
      containers:
        - name: fastapi-app
          image: fastapi-app:0.2
